{
  "_comment": "Reviewed exceptions for scripts/auditGate.mjs. Each entry acknowledges a KNOWN high/critical advisory that cannot be fixed yet, with a reason, reviewer, and EXPIRY (past it the gate treats the advisory as a fresh offender). A brand-new advisory not listed here fails CI immediately. Currently EMPTY: the multer/ws/socket.io/mysql2 debt was cleared by bumping to fixed versions within their existing majors (2026-09), so the gate now enforces a clean tree with no exceptions.",
  "advisories": {}
}
